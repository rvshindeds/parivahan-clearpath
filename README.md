# Parivahan ClearPath

A mobile-first, independent hackathon companion prototype. It has no live connection to Parivahan, VAHAN, SARATHI, banks, or payment gateways.

## Run locally

```bash
npm install
npm run dev
```

Routes: `/welcome`, `/situation-selector`, `/minimal-details`, `/diagnostic`, and `/result`.
